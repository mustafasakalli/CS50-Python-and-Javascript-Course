from dictionaries import ages

ages["Mustafa"] = 21

print(ages)  

# The problem is, it is still executing original "ages" dictionary. But we want to make addition to original file, so don't want to execute original file

